import type { NewsSearchableField } from '../types'

export const DEFAULT_SEARCH_FIELDS: NewsSearchableField[] = ['title', 'ai_summary', 'raw_content', 'source', 'category']

export const DEFAULT_NEWS_LIMIT = 10